<UserControl x:Class="PresentationWpf.Views.TotalSalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D9D9E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style x:Key="FilterCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F7F7FA"/>
            <Setter Property="BorderBrush" Value="#D9D9E3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style x:Key="ReportGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E6E6EE"/>
            <Setter Property="VerticalGridLinesBrush" Value="#F2F2F7"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="36"/>
            <Setter Property="ColumnHeaderHeight" Value="38"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFAFD"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F4F8"/>
            <Setter Property="Foreground" Value="#2B2B35"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderBrush" Value="#E1E3EA"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3F6FB"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DCE8F9"/>
                    <Setter Property="Foreground" Value="#111"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LeftedCellText" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FILTERS + BUTTONS -->
        <Border Grid.Row="0" Style="{StaticResource FilterCardStyle}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Период:"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Margin="0,0,10,0"/>

                <TextBlock Text="с"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0"/>

                <DatePicker SelectedDate="{Binding FromDate, Mode=TwoWay}" SelectedDateFormat="Short"
                            Width="140"
                            Height="30"
                            Margin="0,0,12,0"/>

                <TextBlock Text="по"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0"/>

                <DatePicker SelectedDate="{Binding ToDate, Mode=TwoWay}"  SelectedDateFormat="Short"
                            Width="140"
                            Height="30"
                            Margin="0,0,18,0"/>

                <Button Content="Обновить"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource GrayButton}"
                        Width="110"
                        Height="32"
                        Margin="0,0,10,0"/>

                <Button Content="Сбросить"
                        Command="{Binding ClearPeriodCommand}"
                        Style="{StaticResource GrayButton}"
                        Width="100"
                        Height="32"
                        Margin="0,0,10,0"/>

                <Button Content="Печать"
                        Command="{Binding PrintCommand}"
                        Style="{StaticResource GrayButton}"
                        Width="100"
                        Height="32"
                        Margin="0,0,10,0"/>

            </StackPanel>
        </Border>

        <!-- TITLE -->
        <TextBlock Grid.Row="1"
                   Text="ОТЧЕТ ПО ОБЩИМ ПОКАЗАТЕЛЯМ"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,12,0,10"
                   Foreground="#2B2B35"/>

        <!-- TABLE -->
        <Border Grid.Row="2" Style="{StaticResource ReportCardStyle}">
            <DataGrid ItemsSource="{Binding Rows}"
                      Style="{StaticResource ReportGridStyle}"
                      Margin="0">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Показатель"
                                        Width="280"
                                        Binding="{Binding Name}"
                                        ElementStyle="{StaticResource LeftedCellText}"/>

                    <DataGridTextColumn Header="EURO"
                                        Width="150"
                                        Binding="{Binding Euro, StringFormat=N2}"
                                        ElementStyle="{StaticResource LeftedCellText}"/>

                    <DataGridTextColumn Header="СМН"
                                        Width="150"
                                        Binding="{Binding Smn, StringFormat=N2}"
                                        ElementStyle="{StaticResource LeftedCellText}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
