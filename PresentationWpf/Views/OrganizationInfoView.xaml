<UserControl x:Class="PresentationWpf.Views.OrganizationInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="560">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Height" Value="35"/>
            <!-- a bit taller -->
            <Setter Property="Padding" Value="2,4,2,2"/>
            <!-- space from underline -->
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <!-- larger font -->
            <Setter Property="Margin" Value="0,10,0,20"/>
            <!-- better spacing between rows -->
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                  Margin="0,0,0,2"
                                  Background="{TemplateBinding Background}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Highlight underline on focus -->
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#FF6200EE"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
          

        </Style>
    </UserControl.Resources>


    <StackPanel Margin="20" HorizontalAlignment="Left" Width="500">

        <!-- Title and Save in one line -->
        <DockPanel Margin="0,0,0,20">
            <TextBlock Text="ИНФОРМАЦИЯ ОБ ОРГАНИЗАЦИИ"
                   FontSize="18"
                   DockPanel.Dock="Left"
                   VerticalAlignment="Center"/>
            <Button Content="Сохранить"
                Command="{Binding SaveCommand}"
                Width="120"
                DockPanel.Dock="Right"
                HorizontalAlignment="Right"
                Style="{StaticResource OrangeButton}"/>
        </DockPanel>

        <!-- Fields -->
        <TextBlock Text="Код:"/>
        <TextBox Text="{Binding OrganizationCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Название:"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Адрес:"/>
        <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Город:"/>
        <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Область/Регион:"/>
        <TextBox Text="{Binding Region, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Телефон:"/>
        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Путь экспорта:"/>
        <TextBox Text="{Binding ExportPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <TextBlock Text="Путь импорта:"/>
        <TextBox Text="{Binding ImportPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

    </StackPanel>


</UserControl>
