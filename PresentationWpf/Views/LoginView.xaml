<UserControl x:Class="PresentationWpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationWpf"
             xmlns:views="clr-namespace:PresentationWpf.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewmodels="clr-namespace:PresentationWpf.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LoginViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="350">
    <UserControl.Resources>
        <!-- Define specific styles for LoginView here -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <!-- Add other setters as needed -->
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Width="250" Margin="20">
            <TextBlock Text="Вход" FontSize="24" Margin="0,0,0,10" HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding Message}" Foreground="Red" HorizontalAlignment="Left"/>
            <TextBlock Text="Пользователь:" FontSize="16" Margin="0,0,0,5"  />
            <TextBox Text="{Binding Username}" FontSize="16" Margin="0,0,0,10" Padding="5,8"/>
            <TextBlock Text="Пароль:" FontSize="16" Margin="0,0,0,5" />
            <PasswordBox Name="passwordBox" MaxLength="16" FontSize="16" Margin="0,0,0,20" Padding="5,8"/>

            <Button Style="{StaticResource LoginButtonStyle}" 
                    Content="Логин" FontSize="16" 
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=passwordBox}"
                    Width="100" HorizontalAlignment="Left" Padding="15,7" />
           
        </StackPanel>
    </Grid>
</UserControl>
