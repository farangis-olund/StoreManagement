<UserControl x:Class="PresentationWpf.Views.CoefficientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PresentationWpf.Views"
              xmlns:viewmodels="clr-namespace:PresentationWpf.ViewModels"
            d:DataContext="{d:DesignInstance Type=viewmodels:CoefficientViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="500">

    <UserControl.Resources>
        <!-- Apply to all TextBoxes in this view -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <!-- Стиль для кнопок "Создать" -->
        <Style x:Key="CreateButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3E3B4E"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
           
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                            CornerRadius="6">
                            <ContentPresenter 
                            VerticalAlignment="Center" 
                            HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4E4B60"/>
                            </Trigger>
                            <!-- Pressed -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2D2C34"/>
                            </Trigger>
                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B0B0B0"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <!-- Дневной коэффициент закупа -->
            <GroupBox Header="Коэф. дневного закупа" FontWeight="SemiBold" Margin="0,0,0,15">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Коэф. закупа:" Width="150"/>
                        <TextBox Text="{Binding DailyPurchaseCoefficient}" Width="100"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Количество дней:" Width="150"/>
                        <TextBox Text="{Binding DailyPurchaseDays}" Width="100"/>
                    </StackPanel>
                    <Button Content="Сохранить" Height="35"
                        Command="{Binding SaveDailyPurchaseCommand}"
                        Style="{StaticResource CreateButtonStyle}"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Дневное погашение -->
            <GroupBox Header="Коэф. дневного погашения" FontWeight="SemiBold" Margin="0,0,0,15">
                <StackPanel>

                    <!-- Остаток 1 -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Остаток 1:" Width="80"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokNach1}" Width="80"/>
                        <TextBlock Text="до:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokKon1}" Width="80"/>
                        <TextBlock Text="Дней:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentDays1}" Width="60"/>
                    </StackPanel>

                    <!-- Остаток 2 -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Остаток 2:" Width="80"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokNach2}" Width="80"/>
                        <TextBlock Text="до:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokKon2}" Width="80"/>
                        <TextBlock Text="Дней:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentDays2}" Width="60"/>
                    </StackPanel>

                    <!-- Остаток 3 -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Остаток 3:" Width="80"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokNach3}" Width="80"/>
                        <TextBlock Text="до:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokKon3}" Width="80"/>
                        <TextBlock Text="Дней:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentDays3}" Width="60"/>
                    </StackPanel>

                    <!-- Остаток 4 -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Остаток 4:" Width="80"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokNach4}" Width="80"/>
                        <TextBlock Text="до:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokKon4}" Width="80"/>
                        <TextBlock Text="Дней:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentDays4}" Width="60"/>
                    </StackPanel>

                    <!-- Остаток 5 -->
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Остаток 5:" Width="80"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokNach5}" Width="80"/>
                        <TextBlock Text="до:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentOstatokKon5}" Width="80"/>
                        <TextBlock Text="Дней:" Margin="10,0,5,0"/>
                        <TextBox Text="{Binding DailyRepaymentDays5}" Width="60"/>
                    </StackPanel>

                    <Button Content="Сохранить" Height="35"
                        Command="{Binding SaveDailyRepaymentCommand}"
                        Style="{StaticResource CreateButtonStyle}"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Дневной запланированный закуп -->
            <GroupBox Header="Коэф. дневного запланированного закупа" FontWeight="SemiBold" Margin="0,0,0,15">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="% коэф.:" Width="150"/>
                        <TextBox Text="{Binding DailyPlannedPurchaseCoefficient}" Width="100"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="Количество дней:" Width="150"/>
                        <TextBox Text="{Binding DailyPlannedPurchaseDays}" Width="100"/>
                    </StackPanel>
                    <Button Content="Сохранить" Height="35"
                        Command="{Binding SaveDailyPlannedPurchaseCommand}"
                        Style="{StaticResource CreateButtonStyle}"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>



</UserControl>
